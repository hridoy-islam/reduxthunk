// import { READING_COUNT } from "../actionTypes/actionTypes";

// const postMiddleware = store => next => action => {

   

//     if (action.type === READING_COUNT) {
//         const newAction = {
//             ...action,
//             payload: { ...action.payload, reading: 1 },
//         };

//         return next(newAction);
//     }

//     return next(action);

// }

// export default postMiddleware;